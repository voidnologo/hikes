# Carousel & site images

Drop landing-page carousel photos here, then reference them in `/index.html`
(see the 3-step instructions at the top of `/assets/js/carousel.js`).

Per-hike images live inside that hike's own folder under `hikes/<hike>/`,
not here.
